<template>
    <div class="wrapper">
        <div class="card">

            <!-- REVIEWING SECTION -->
            <template if:true={isReviewing}>
                <div class="section fade-in">
                    <div class="loader"></div>

                    <h2 class="title">We are reviewing your credit card application</h2>
                    <p class="subtitle">Please wait while we verify your details...</p>

                    <!-- Video -->
                    <video class="videoBox" autoplay muted playsinline>
                        <source src={videoUrl} type="video/mp4" />
                        Your browser does not support the video tag.
                    </video>
                </div>
            </template>

            <!-- FINAL DECISION -->
            <template if:true={isDecisionReady}>
                <div class="section fade-in">

                    <template if:true={isApproved}>
                        <div class="statusIcon approved">✔</div>
                        <h2 class="approvedTxt">Approved!</h2>
                        <p>Your credit card application has been successfully approved.</p>
                    </template>

                    <template if:true={isDeclined}>
                        <div class="statusIcon declined">✖</div>
                        <h2 class="declinedTxt">Declined</h2>
                        <p>We’re sorry. Based on our review, we cannot approve your request.</p>
                    </template>

                    <template if:true={isMoreTime}>
                        <div class="statusIcon pending">⏳</div>
                        <h2 class="pendingTxt">We Need More Time</h2>
                        <p>Your application requires additional verification. Please check back soon.</p>
                    </template>

                </div>
            </template>

        </div>
    </div>
</template>
